aiogram==3.13.1
aiosqlite==0.20.0
apscheduler==3.10.4
python-dotenv==1.0.1
cryptography==43.0.0
aiohttp==3.10.5
telethon==1.36.0

# Admin password hashing (ADMIN_PASSWORD_HASH).
# Dev tools (ruff, pytest) live in requirements-dev.txt
bcrypt==4.2.0

# zoneinfo on Windows has no system IANA database and needs this package.
# The platform marker skips it on Linux/macOS (Railway etc.).
tzdata>=2025.1; sys_platform == "win32"
